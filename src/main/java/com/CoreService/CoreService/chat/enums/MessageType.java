package com.CoreService.CoreService.chat.enums;

/**
 * Kind of payload a message carries. SYSTEM is produced by the server only.
 */
public enum MessageType {
    TEXT,
    IMAGE,
    FILE,
    SYSTEM
}
