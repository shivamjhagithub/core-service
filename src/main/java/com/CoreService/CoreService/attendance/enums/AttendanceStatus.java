package com.CoreService.CoreService.attendance.enums;

/**
 * PRESENT and LATE both count as attended when a percentage is computed.
 */
public enum AttendanceStatus {
    PRESENT,
    ABSENT,
    LATE,
    EXCUSED
}
