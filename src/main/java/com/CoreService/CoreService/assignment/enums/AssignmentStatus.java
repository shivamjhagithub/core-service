package com.CoreService.CoreService.assignment.enums;

/**
 * Lifecycle of an assignment. Students only ever see {@code PUBLISHED} and
 * {@code CLOSED} work.
 */
public enum AssignmentStatus {
    DRAFT,
    PUBLISHED,
    CLOSED
}
