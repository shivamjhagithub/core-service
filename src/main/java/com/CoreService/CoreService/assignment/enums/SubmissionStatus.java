package com.CoreService.CoreService.assignment.enums;

/**
 * {@code NOT_SUBMITTED} describes a student who has no submission row yet; it is
 * never persisted.
 */
public enum SubmissionStatus {
    NOT_SUBMITTED,
    SUBMITTED,
    LATE,
    GRADED
}
